# LiftOff
LiftOff generates clean project scaffolds from a simple prompt — Flask apps, APIs, RAG demos, whatever. No hallucinated junk, just real folder structures, starter code, configs, and TODOs. Uses OpenAI or HF backends with validation
